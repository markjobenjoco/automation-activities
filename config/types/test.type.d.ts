/**
 * Test options
 *
 * @property {string} [message] - Optional message to display during validation
 * @property {boolean} [toValidate] - Flag indicating whether to perform validation
 */
export type TestOptions = {
  /**
   * Optional message to display during validation
   */
  message?: string

  /**
   * Flag indicating whether to perform validation
   */
  toValidate?: boolean
}
